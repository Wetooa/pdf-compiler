Producer-Consumer Sample 
Implementation
shared_data.c
#define BUFFER_SIZE 10
typedef struct {
    //. . .
} item;
item 
buffer[BUFFER_SIZE];
int in = 0;
int out = 0;
item nextProduced;
while (1) {
 while (((in + 1) % 
    BUFFER_SIZE) == out);  
   /* do nothing */
 buffer[in] = 
nextProduced;
 in = (in + 
1)%BUFFER_SIZE;
}producer .c
item nextConsumed;
 while (1) {
   while (in == out);
    /* do nothing */
   nextConsumed = 
buffer[out];
   out =(out + 
1)%BUFFER_SIZE;
}consumer .c
Process P1Process P2IPC through Shared Memory (Physical)
BUFFER